# 依赖
node_modules/
.bun/

# 构建产物
dist/
build/
*.cjs
*.exe
*.dmg
*.AppImage
!apps/electron/scripts/*.cjs

# 调试/临时
*.log
.dev/
.env
.env.local
*.bak
*.tsbuildinfo

# 编辑器
.vscode/
.idea/
*.swp

# 系统
.DS_Store
Thumbs.db

# Electron
out/
electron-dist*/
out-local*/
release-notes/__generated/

# 临时实验（docs/dev/ 可定期清，但先保留）
# docs/dev/ 可手动清

# kscc plan 文件目录（.claude 产物，不进库）
.context/

# mimo-ai 工具运行产物目录（自带 .gitignore 排除 node_modules 等，整体忽略）
.mimicode/
.mimicode/**
# 显式补齐 mimo 自带 gitignore 故意保留、但不属于本项目的 3 项
.mimicode/distill/
.mimicode/dream_inspect.py
.mimicode/skills/

# mimo-ai 工具产物（skills/distill 等）
.mimocode/

# Claude 本地会话配置（agent 产物，不入库）
.claude/

# BEGIN KCWork auto-injected skill symlinks (managed — do not commit)
.claude/skills/cron
.claude/skills/kcwork-config
.claude/skills/officecli
.claude/skills/skill-creator
# END KCWork auto-injected skill symlinks
