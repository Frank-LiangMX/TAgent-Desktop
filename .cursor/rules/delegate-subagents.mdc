---
description: 复杂任务必须派 Task 子代理；总监编排 + 规格/checkpoint，勿事事亲力亲为
alwaysApply: true
---

# 子代理优先（总监模式）

本仓库复杂工作时，主 agent 默认当**总监**：拆目标、派子代理、交叉验收、写规格与 checkpoint。不要等用户再说「派子代理」。

## 何时必须派 Task 子代理

满足任一条件即派，可并行多路：

- 只读摸底 / 根因调查 / 对照其他仓库（`explore`）
- 机械实现或范围清晰的改动（`generalPurpose` / `shell`）
- 需要对照 `F:\TAgent_General`、`F:\Proma` 或本仓库多处逻辑
- 用户要「规范 / checkpoint / 文档 / 验证」类整包交付

窄问题（单文件几行、已有明确补丁）可自己做，不必为派而派。

## 怎么派

- 用 Cursor **Task** 工具；探索用 `subagent_type: "explore"`，实现用 `generalPurpose`，命令用 `shell`
- 独立调查**并行**启动，不要串行干等
- prompt 写清：目标、路径、验收、**不要改什么**；要求返回「文件+结论」，禁止空泛复述
- 子代理结果只取结论与证据，主 agent 负责整合规格与落地优先级

## 交付节奏（与本仓库 streaming-rework 一致）

1. 先写/更新 `docs/dev/**` 规格（根因、契约、验收、本轮不做）
2. 按工作流改代码 + 单测
3. 用户要求或规格写明时再 `git commit` checkpoint；不擅自 push

## 模型

- 探索/对照类子代理优先用更快、更便宜的模型（若 Task 的 `model` 可选）
- 架构拍板、消闪空类契约、最终整合由主 agent 把关

## 反例

- ❌ 大范围自己线性翻代码，用户提醒才开子代理
- ❌ 子代理无验收标准、改完不写规格
- ❌ 缺错误原文就重写运行核（见 `01-CHECKPOINT2-SPEC` W7）
