---
description: 总监编排；探索/杂活/实现一律派本地 kscc CLI，禁止 Cursor Task 烧额度
alwaysApply: true
---

# 子代理优先（总监模式）

本仓库复杂工作时，主 agent 默认当**总监**：拆目标、写 brief、用**本地 kscc CLI**派工、交叉验收。不要等用户再说「派子代理」。

**用户硬约束（额度）：** 探索、摸底、杂活、对照、实现 —— **一律用本机 `kscc` 子代理**，不要用 Cursor 自带的 `Task` / explore / composer 子代理顶替。用户不想消耗 Cursor 额度。

## 何时必须派本地 kscc

满足任一条件即派，可并行多路（Shell 拉起多进程）：

- 只读摸底 / 根因调查 / 对照 `F:\TAgent_General`、`F:\Proma` / 本仓库多处逻辑
- 机械实现或范围清晰的改动
- 规范 / checkpoint / 文档 / 验证类整包交付中的执行部分
- 用户明确说的「杂活」「探索」

窄问题（单文件几行、已有明确补丁）可总监自己做，不必为派而派。

## 怎么派（强制）

```bash
# 仓库根目录；headless；可后台并行
kscc -p --dangerously-skip-permissions "<读 docs/dev/.../xxx-brief.md 并按验收执行；返回文件+结论>"
```

可选：`--agent <name>` / `--agents '<json>'` 定义专用子代理角色。

流程：

1. 总监只写 `docs/dev/**` 规格与 `*-brief.md`
2. 用 **Shell** 拉起 `kscc -p ...`（可 `block_until_ms: 0` 后台并行）
3. 子代理结果只取结论与证据；总监交叉验收、整合落地
4. **禁止**用 Cursor `Task`（explore / generalPurpose / composer）做探索或实现来「省事」——烧的是用户 Cursor 额度
5. Cursor `Task` **仅**在用户明确要求 IDE 内辅助，或纯 `shell` 编排且不调模型时可用

## 其它本地渠道（次选）

| 渠道 | 何时用 |
|------|--------|
| **kscc** | **默认**：探索、杂活、对照、核心实现、验收 |
| grok / mimo / opencode | 仅当用户当次指定，或 kscc 不可用时的备胎 |

## 交付节奏

1. 先写/更新 `docs/dev/**` 规格（根因、契约、验收、本轮不做）
2. 本地 kscc 按 brief 改代码 + 单测
3. 用户要求或规格写明时再 `git commit` checkpoint；不擅自 push

## 反例

- ❌ 用 Cursor Task/explore/composer 顶替本地 kscc（烧错额度）
- ❌ 大范围自己线性翻代码，用户提醒才开子代理
- ❌ 子代理无验收标准、改完不写规格
- ❌ 缺错误原文就重写运行核（见 `01-CHECKPOINT2-SPEC` W7）
