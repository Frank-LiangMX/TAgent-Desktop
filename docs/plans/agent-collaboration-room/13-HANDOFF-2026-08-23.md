# 融合会话长期实现交接文档

> 交接日期：2026-08-23
> 当前基线：`main`，起点为 `v2.0.0-dev.5`（提交 `73e6dff`）；本次交接包含其后的本地改动。
> 用途：让另一台设备、另一位 Agent 或另一套模型能够在不重新阅读全部历史讨论的情况下继续开发。

## 1. 先看什么

继续开发前按以下顺序阅读：

1. 本文件：当前真实状态、代码入口、验证命令和剩余任务。
2. [03-IMPLEMENTATION-PHASES.md](./03-IMPLEMENTATION-PHASES.md)：阶段目标、风险和验收门。
3. [06-MULTIUSER-FUSION-IMPLEMENTATION.md](./06-MULTIUSER-FUSION-IMPLEMENTATION.md)：普通会话、融合会话、Bot、Sidecar、多用户和工作区的统一规格。
4. [02-RUNTIME-A2A-SPEC.md](./02-RUNTIME-A2A-SPEC.md)：RoomSession、Run、Mailbox、A2A 和恢复约束。
5. [12-IMPLEMENTATION-LOG-2026-08-22.md](./12-IMPLEMENTATION-LOG-2026-08-22.md)：按时间顺序的实现记录，最新条目为 §83（P0-3c Electron 启动接通 FusionRoom transport：据闸门 + TLS bootstrap + 只读 transport:status IPC + 设置页一行只读，默认仍关）。
6. [05-PERSISTENT-BOTS-AND-ROOM-SEATS.md](./05-PERSISTENT-BOTS-AND-ROOM-SEATS.md)：持久 Bot、席位副本和 Bot 记忆边界。

历史实现日志保留历史语境，不要把历史条目中的“未完成”直接当作当前状态；当前状态以本文件和 `03` 的“当前交接基线”为准。

## 2. 产品结论

用户面对的是统一的“会话”，而不是三套互相割裂的入口：

| 场景 | 用户感知 | 运行时边界 |
| --- | --- | --- |
| 没有 Bot | 普通会话 | 现有单 AgentSession / 单 Run |
| 加入 1 个 Bot | 直接和这个 Bot 对话 | 尽量复用普通会话的单成员路径 |
| 加入 2 个以上 Bot | 融合会话 | RoomSession、协调者、任务和 A2A |
| 多个用户加入 | 多人融合会话 | 服务端权威 RoomSession |
| 正式会话临时 @ 未加入 Bot | Bot 悬浮侧窗 | 独立 SidecarSession，通过 proposal bridge 回主会话 |

关键交互规则：

- 第一个 Bot 可以直接承接普通消息。
- 加入第二个 Bot 后，第一个仍在席位中的 Bot 默认是协调者。
- 用户没有 `@` 时交给协调者；用户明确 `@` 时路由到指定 Bot。
- Bot 内部协作使用受控 mailbox/task/approval，不把隐藏 prompt 或内部思维链直接写入公共时间线。
- 侧窗可以读取主会话的公开投影，但只能通过桥接提案请求主会话采用，不能直接写主会话消息。
- 加入房间的 Bot 是当时配置的 `RoomBotSeat` 副本，不是 BotProfile 本体；删除席位不删除 BotProfile。
- Bot 的长期记忆是小而精炼的持久知识：candidate 不进入 prompt，只有用户确认后的 active memory 才允许检索。

## 3. 当前已经落地的真实能力

### 3.1 本地统一会话和 Bot

- 普通会话、融合会话、Bot sidecar 使用同一套主 UI 入口和主题系统。
- 角色库与 Bot 库分开：角色卡仍然是角色/提示配置，Bot 是拥有配置版本、模型/渠道、工具能力和长期记忆的可复用实体。
- Bot Library 已有配置副本和 candidate/active 记忆边界；加入融合会话时固定 `configRevisionId` 和快照字段。
- Sidecar 支持打开、关闭、最小化、吸边、拖拽和通过 IPC 把 proposal 交给主会话；侧窗不直接污染主会话时间线。

### 3.2 Room authority

`packages/core/src/collaboration/fusion-room-authority.ts` 是 RoomSession 的领域真值，当前快照包含：

- roomId、ownerUserId、status、title、goal；
- humanMembers、botSeats、botOwnerConsents、coordinatorSeatId；
- workspace、messages、events、usage；
- files、locks、runs；
- tasks、artifacts、approvals、mailbox。

已实现并有测试的 authority 能力：

- 人类成员邀请、接受、离开、移除、presence；
- Bot seat 加入、移除、owner consent、协调者继任；
- 用户消息和成员消息，目标成员、回复链、rootMessageId、幂等；
- title/goal owner-only 更新，旧快照兼容恢复；
- task 创建、严格状态迁移、版本 CAS、幂等；
- artifact 发布，路径安全、内容大小、sha256/byteSize、task 关联；
- 用户审批 request/resolve；
- A2A mailbox send/reply，深度、自环、root、causation、requestId 和 payload 限制；
- run start/await/finish，fence、triggerMessageId、awaiting_peer、awaiting_user、blocked；
- usage 账本和 Bot owner 费用归属字段；
- workspace 短锁、expected SHA、单文件/批量提交；
- workspace delete/move：删除 tombstone、移动双锁、拒绝覆盖、幂等。

### 3.3 Host、Gateway 和远程 RoomSession

- `packages/core/src/collaboration/fusion-room-host.ts`：按 roomId 管理 authority，统一 dispatch、snapshot save、事件通知和工作区事务回滚。
- `packages/core/src/collaboration/fusion-room-gateway.ts`：远程 wire action；`actorUserId` 由 authenticated principal 注入，renderer/wire payload 不能伪造身份；幂等键按动作和用户隔离。其 `defaultAuthorize` 已委托 P0-1 协议层 `decideRoomAccess`（见 `fusion-room-acl.ts`）。
- `packages/core/src/collaboration/fusion-room-http-client.ts`：远程快照、action、SSE cursor 和已发布文件下载。
- `apps/electron/src/main/lib/collaboration/fusion-room-runtime.ts`：显式组装 snapshot store、invite token、workspace store、HTTP/SSE transport、execution bridge 和默认 host tools。
- `apps/electron/src/main/lib/collaboration/fusion-room-http-server.ts`：认证后才允许读写；文件下载只允许 authority 明确 `downloadable === true` 且未删除的文件。
- 远程页面只通过 `FusionRoomViewModel` 和 `FusionRoomActionAdapter` 消费；页面不持有 authority、不接收 actorUserId、不做调度。
- 打包版网络入口当前仍然关闭。不要为了“先能连”而打开未经真实账户认证、证书生命周期和跨用户 ACL 设计的公网入口。
  - **旁注（2026-08-23，P0-3 证书 + 显式闸门已落地）**：`apps/electron/src/main/lib/collaboration/fusion-room-cert-store.ts` 已交付 RoomSession 服务端 TLS 证书的本地生命周期（generate/load/rotate/revoke，纯 `node:crypto` 自签，无外部 CA 库）；`fusion-room-network-prefs.ts` 已交付 `decidePackagedCollaborationGate` 决策函数 + 持久化偏好；`main/index.ts` 已用该函数替代硬编码 `!app.isPackaged`。**默认 prefs 全关 → 打包行为不变（协作室 IPC 不注册、非 loopback 不监听）**；仅当用户显式打开 `enableCollaboration` 才注册协作室 IPC，`enableNetworkListen` 还需存在 active 且未过期 TLS 证书。真实账户 / OAuth / 跨机器 E2E / 生产 CA / 远程 transport 从 Electron 启动仍未做，详见 [12-IMPLEMENTATION-LOG §74](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
  - **旁注（2026-08-23，P0-3b 设置页危险区 UI 已落地，默认仍关）**：设置页「关于」底部已挂「融合会话 · 协作与网络（危险区）」——`enableCollaboration` / `enableNetworkListen` 开关 + 证书列表 / 生成 / 撤销 + 只读闸门状态卡（含待重启徽章）；新增 `fusion-room-network-prefs:gate-status` IPC（`getFusionRoomGateStatus` 经 preload + App.tsx 暴露）。**应用策略选 B**：闸门变更提示重启生效，不动态注册 / 卸载 IPC；**默认仍全关、不自动起非 loopback 监听**；未做实机点击手测，以组件模型单测 + typecheck 验证。详见 [12-IMPLEMENTATION-LOG §75](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
  - **旁注（2026-08-23，P0-3c transport 启动接通已落地，默认仍关）**：`apps/electron/src/main/lib/collaboration/fusion-room-transport-bootstrap.ts` 交付 `bootstrapFusionRoomTransport`——据显式闸门 + cert store 的 active TLS 材料决定是否 `createFusionRoomTransportRuntime` 并 `start`（`!allowNonLoopbackListen` → `disabled`；`allowNonLoopbackListen` + TLS → HTTPS 非 loopback `listening`；无 TLS → `failed`，不得明文非 loopback；可选 `enableDevLoopbackTransport` 仅强制 127.0.0.1 明文）。`main/index.ts` 删除「仅 console.log 占位」改为真实 bootstrap + 日志 + `before-quit` close；新增 `fusion-room-transport:status` 只读 IPC（`appliedGate` + transport 投影，剥离 runtime 句柄），preload + App.tsx 同步 `getFusionRoomTransportStatus`，设置页危险区加一行只读「传输：…」。`enableDefaultMemberExecution` 始终 false；**默认仍关**；**未做**真实账户 OAuth / 跨机器实机 E2E / 热插拔 listen / 实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §83](./12-IMPLEMENTATION-LOG-2026-08-22.md)。

### 3.4 执行桥和 Bot 工具

`apps/electron/src/main/lib/collaboration/fusion-room-execution-bridge.ts` 当前支持：

- 用户消息按 `@`、coordinator、首个 Bot 路由；
- member-message、send-mailbox、resolve-approval、reply-mailbox 触发后续 Bot；
- Bot A `room_ask` -> A 等待 -> Bot B 被触发 -> B `room_reply` -> A continuation；
- `waitForIdle()` 动态 drain，等待运行期间新加入的 continuation；
- `FusionRoomHost.recoverInterruptedRuns()` 在 runtime 启动时把上次进程仍为 running 的 run 安全标成 blocked，不自动重放可能已经发生外部副作用的 run。

  - **旁注（2026-08-23，P1-2c 生产路径注入 lifecycle 已落地）**：`CollaborationRoomService` 默认栈与 Fusion runtime 显式 opt-in 现共享同一「带 lifecycle 的 ChannelBackendAdapter」工厂 `createDefaultChannelMemberStack()`（`member-backend-factory.ts`，adapter 已 `bindTurnAbort` 到 lifecycle）。`CollaborationRoomService` 默认装配该栈——生产 `registerCollaborationRoomIpc` 路径自动生效，`cancelRun` running 分支调 `lifecycle.interruptSession({ handle:{ sessionId: member.logicalSessionId, ... }, reason:'cancel-run' })` 取消进行中的 turn（fire-and-forget，忽略 NOT_FOUND）；只注入 adapter 不注入 lifecycle 的 legacy 配置保持旧行为，兼容既有测试。Fusion runtime 新增 `enableDefaultMemberExecution?: boolean`（**默认 false**）：为 true 且未传 memberAdapter 时自动装配默认栈 + executionBridge，远程 / 打包 transport 默认仍 authority-only 不自动开执行 / 网络。**未做**：native resume、createSession-before-every-turn（cancel 经 bindTurnAbort+interruptSession 仍可取消 turn，但 heartbeat 对未 createSession 的 session 返回 unknown）、持久 outbox worker、实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §80](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
  - **旁注（2026-08-23，P1-3 持久 outbox worker 已落地，安全 drain、不自动重放）**：`apps/electron/.../fusion-room-outbox-worker.ts` 交付 `FusionRoomOutboxWorker` + 纯函数 `classifyOutboxDrain`——在 `recoverInterruptedRuns` 之后扫描各房间 continuation，仅对 `approved_awaiting_resume`（bridge `resolve-approval` 拉新 turn）与 `mailbox_outbox` 且 `delivery==='outbox'`（房主 actor `confirmResumeContinuation` 推进 dispatched + bridge 唤醒 toMember）安全自动 drain；`blocked_run` / `pending_approval` / `depth_stop` / 已 dispatched 信封一律 observe、绝不自动重放。已 drain 键 `roomId:kind:continuationId` 原子持久化（`atomic-json`，默认 `getCollaborationDir()/fusion-outbox-worker.json`），避免重启双开。`fusion-room-runtime.ts` 在 recover 后构造 worker 并 `drainAll()` 一次，挂到 `runtime.outboxWorker`（始终挂载，无 bridge 时仅 scan/observe）；新增可选 `outboxWorkerStatePath`；**不默认开短轮询**。与 legacy `CollaborationRoomService.recoverInterruptedRuns` 的 outbox 安全重投语义对齐（都只重投尚未启动模型调用的 outbox 信封），但本切片不改 legacy 路径。未做：legacy service 共用 worker、IPC 暴露 scan、轮询默认开、真实 provider resume、实机手测。详见 [12-IMPLEMENTATION-LOG §81](./12-IMPLEMENTATION-LOG-2026-08-22.md)。

`apps/electron/src/main/lib/collaboration/fusion-room-host-tools.ts` 当前工具：

- `room_send`、`room_ask`、`room_reply`；
- `room_task_assign`、`room_task_update`、`room_publish_artifact`、`room_request_user`；
- `workspace_read_file`、`workspace_search`、`workspace_write_file`、`workspace_apply_patch`、`workspace_run_command`；
- `workspace_delete_file`、`workspace_move_file`。

工具的 roomId、seatId、runId、fence、actor 和权限由闭包/authority 提供，模型只能提供业务参数。写工具必须满足 Bot 的 `workspace-write` 权限、owner consent、当前 running run 和有效锁。

### 3.5 工作区和产物

- `apps/electron/src/main/lib/collaboration/fusion-room-workspace-store.ts` 将 RoomWorkspace materialize 到房间独立目录。
- 路径必须是安全相对路径；禁止 `..`、绝对路径、反斜杠和符号链接穿越。
- 写入使用临时文件和 atomic rename；delete 只允许普通文件；move 拒绝覆盖目标并支持 rollback。
- authority 的 deleted 文件只保留 tombstone 供冲突保护；renderer 和下载接口过滤 tombstone。
- 其他用户要获得实际产出，使用服务端已发布文件的下载，而不是默认反向同步到个人磁盘。

## 4. 重要安全不变量

以下规则不能为了测试方便或“先跑起来”而放宽：

1. renderer 不是调度器，也不是任何真值源。
2. 普通会话升级为多人融合会话时派生 RoomSession，不原地改变私有会话可见性。
3. Bot seat 是配置副本；BotProfile 的私有历史和 active memory 不因入房间公开。
4. Bot owner consent、room policy、task scope、resource grant 和运行权限取交集。
5. 所有公共消息、任务、产物、审批、mailbox 和文件元数据都必须经过 authority/Host。
6. 用户/网络输入不能提供 actorUserId、room owner、fence、depth、causation 等安全字段。
7. A2A 必须有 root、causation、深度和预算边界；禁止自环和无限互聊。
8. 文件必须经过安全路径、锁、expected SHA 和事务；禁止工具直接写物理路径。
9. 无法确认外部副作用时，重启恢复必须标记 blocked 或等待用户决定，不自动无限重放。
10. 未确认 candidate memory、侧窗未确认内容、私聊历史和未授权文件不能进入 Bot prompt。

## 5. 关键代码地图

```text
packages/core/src/collaboration/
  fusion-room-authority.ts          authority、快照、状态机、锁、run、A2A
  fusion-room-host.ts                dispatch、持久化、事件、物理事务
  fusion-room-gateway.ts             authenticated remote wire action
  fusion-room-http-client.ts         remote HTTP/SSE client
  fusion-room-session-adapter.ts     transport-neutral session adapter

apps/electron/src/main/lib/collaboration/
  fusion-room-runtime.ts             显式 runtime 装配
  fusion-room-execution-bridge.ts   Bot turn、路由、A2A/审批 continuation
  fusion-room-host-tools.ts         默认安全工具 factory
  fusion-room-workspace-store.ts    房间目录和物理文件事务
  fusion-room-http-server.ts        HTTP/SSE、认证、下载
  fusion-room-snapshot-store.ts     本地快照
  fusion-room-invite-token-store.ts invite token
  fusion-room-workspace-lease-store.ts 共享工作区租约
  collaboration-room-service.ts     legacy/local 协作室服务，继续复用

apps/electron/src/renderer/components/collaboration/
  CollaborationRoomsPage.tsx        本地房间 + 显式远程连接入口
  FusionRoomRemotePage.tsx          远程页面，使用现有主题
  fusion-room-view-model.ts         authority -> immutable UI projection
  fusion-room-action-adapter.ts     不接收 actorUserId 的 action facade
  fusion-room-remote-session.ts     远程 session/controller 装配

apps/electron/src/main/lib/bot/
  BotProfile / config revision / memory candidate-active 相关持久化与服务

packages/shared/src/types/
  collaboration-room.ts             room/member/run/mailbox/tool 类型
  fusion-session.ts                  普通/融合会话统一路由类型
  fusion-routing.ts                 0/1/2+ Bot 路由策略
```

## 6. 验证命令

在仓库根目录 `F:\TAgent-Desktop` 执行：

```powershell
bun run typecheck
bun run build
bun test packages/core/src/collaboration/fusion-room-authority.test.ts
bun test packages/core/src/collaboration/fusion-room-host.test.ts packages/core/src/collaboration/fusion-room-gateway.test.ts
bun test apps/electron/src/main/lib/collaboration/fusion-room-workspace-store.test.ts
bun test apps/electron/src/main/lib/collaboration/fusion-room-execution-bridge.test.ts
bun test apps/electron/src/renderer/components/collaboration/fusion-room-view-model.test.ts apps/electron/src/renderer/components/collaboration/fusion-room-action-adapter.test.ts
bun test packages/core/src/collaboration/fusion-room-http-client.test.ts packages/core/src/collaboration/fusion-room-session-adapter.test.ts

git diff --check
```

已知环境事实：

- `bun run typecheck` 和 `bun run build` 是当前最稳定的全仓验证；build 只保留既有动态 import 和大 chunk warning。
- Electron 主进程部分测试有 safeStorage ESM mock 环境限制，不能把直接 Bun 加载失败误判为业务失败，应使用项目 Electron/Vitest 入口或依赖 typecheck/build。
- `kscc` 可以运行 doctor 和 CLI；本轮用 `kscc -p --bare --model glm-5.2` 做窄审查未返回文本，因此没有把无输出当作审查结论。

## 7. 当前明确未完成项

按优先级排列：

### P0：跨用户可用之前必须完成

1. 真实账户认证、Room ACL、邀请 token 与账户身份绑定。
   - **旁注（2026-08-23，P0-1 协议层已落地）**：`packages/core/src/collaboration/fusion-room-acl.ts` 已交付纯协议层 ACL 判定（`decideRoomAccess` / `decideBotRuntimeAccess` / `decideResourceAccess` / `resolveBillingSubject`）+ 单测；gateway `defaultAuthorize` 已委托 `decideRoomAccess`，行为保持兼容。**真实账户登录 / OAuth / 证书生命周期 / 邀请 token 与账户身份绑定 / 跨机器 E2E 仍未做**，详见 [12-IMPLEMENTATION-LOG §72](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P0-2 本地 fixture 已落地）**：`apps/electron/src/main/lib/collaboration/fusion-room-multiuser-fixture.test.ts` 已用 loopback HTTP·SSE fixture 跑通双用户 / 双 Bot owner / 共享工作区 / 邀请 token 身份（含 `principal.roomId` → `SCOPE_MISMATCH`）/ Bot owner consent / 费用主体归属 / 可下载产物 E2E + SSE 加分项；`FileFusionRoomInviteTokenStore.authenticate` 已注入 `principal.roomId`，无需为本切片补线。**真实账户 / 证书 / 跨机器 / 打包公网入口仍未做**，详见 [12-IMPLEMENTATION-LOG §73](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
2. 证书生成/轮换/撤销、服务端部署配置、打包版显式网络启动 UI。
   - **旁注（2026-08-23，P0-3 已落地）**：证书本地生命周期（generate/load/rotate/revoke，纯 `node:crypto` 自签 + atomic-json 持久化）与打包版显式闸门 `decidePackagedCollaborationGate`（默认全关；`enableCollaboration` 控 IPC，`enableNetworkListen` 还需 active 证书）已交付，`main/index.ts` 已接入；撤销 / 过期证书不进 `resolveTlsOptions`，`allowInsecureNetwork` 等明文公网开关被显式拒绝。**未做**：生产 CA / 真实账户、服务端部署配置、远程 transport 从 Electron 启动接通 `allowNonLoopbackListen`。详见 [12-IMPLEMENTATION-LOG §74](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P0-3b 设置页危险区 UI 已落地，默认仍关）**：设置页「关于」底部已挂「融合会话 · 协作与网络（危险区）」——`enableCollaboration` / `enableNetworkListen` 开关（后者在 `!enableCollaboration` 或 `!hasActiveCert` 时 disabled）+ 证书列表 / 生成 / 撤销 + 只读闸门状态卡（含待重启徽章）；新增 `fusion-room-network-prefs:gate-status` IPC（`getFusionRoomGateStatus` 经 preload + App.tsx 暴露）。**应用策略选 B**：闸门变更提示重启生效，不动态注册 / 卸载 IPC。**默认仍全关、不自动起非 loopback 监听**；未做实机点击手测（无 Electron GUI），以组件模型单测 + typecheck 验证。详见 [12-IMPLEMENTATION-LOG §75](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P0-3c transport 启动接通已落地，默认仍关）**：`bootstrapFusionRoomTransport`（`fusion-room-transport-bootstrap.ts`）据闸门 + cert store 的 active TLS 材料决定是否 `createFusionRoomTransportRuntime` 并 `start`——`allowNonLoopbackListen` + TLS → HTTPS 非 loopback `listening`；无 TLS → `failed`（不得明文非 loopback）；`!allowNonLoopbackListen` → `disabled`（可选 `enableDevLoopbackTransport` 仅强制 127.0.0.1 明文）。`main/index.ts` 删占位改真实 bootstrap + 日志 + `before-quit` close；新增 `fusion-room-transport:status` 只读 IPC + preload / App.tsx `getFusionRoomTransportStatus` + 设置页一行只读「传输：…」。`enableDefaultMemberExecution` 始终 false。**默认仍关**；**未做**真实账户 OAuth / 跨机器实机 E2E / 热插拔 listen / 实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §83](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
3. 多用户跨机器 E2E：不同用户、不同 Bot owner、不同费用主体、共享工作区访问和下载。
   - **旁注（2026-08-23，P0-2）**：同一台机器 / 同一进程 / loopback 的双用户 fixture E2E 已有（见上条旁注与 §73）；**跨机器**（不同设备、真实账户、TLS 证书）E2E 仍未做。
4. 跨节点持久化、单写者/乐观并发、事件总线和预算原子预留。

### P1：融合会话生产化

1. 真实 Pi/KSCC/provider 进程的 adapter contract、create/resume/compact/interrupt、心跳和 usage 回写。
   - **旁注（2026-08-23，P1-2a 契约 + Fake + Channel fail-closed 已落地，真机 E2E 未做）**：`packages/shared/src/types/member-session-lifecycle.ts` 已交付统一生命周期契约（`MemberSessionLifecycleAdapter`：create/resume/compact/interrupt/heartbeat）+ `MemberSessionBackend` / `MemberSessionResumeMode` / `MemberSessionHandle` 等类型 + `normalizeMemberTurnUsage` 纯函数 + 单测；`apps/electron/.../member-session-lifecycle.ts` 交付 `ChannelMemberSessionLifecycleAdapter`（诚实 fail-closed：`supportsResume=false` → resume 抛错、compact 返回 not implemented、interrupt 仅内存登记未接进程级 kill、heartbeat 据已知/中断状态）+ `MemberSessionLifecycleError`；`.../member-session-lifecycle-fake.ts` 交付 `FakeMemberSessionLifecycleAdapter`（单测主力，无 I/O，可配 resume/supportsResume/compact 失败）。组合别名 `MemberBackendWithLifecycleAdapter = MemberBackendAdapter & MemberSessionLifecycleAdapter`。**未做**：真机 kscc/Pi create→resume E2E（P1-2b）、进程级 kill、bridge 全量改用 normalize（已导出 + 测，接线留后）、实机手测。详见 [12-IMPLEMENTATION-LOG §78](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P1-2b interrupt 接 AbortSignal + bridge 接 normalize + 门控真机冒烟已落地，不声称 native resume）**：`MemberSessionLifecycleAdapter` 新增 `bindTurnAbort(sessionId, callerSignal): AbortSignal`（组合调用方 signal 与 session interrupt controller）；`ChannelMemberSessionLifecycleAdapter` / Fake 实现 `bindTurnAbort` + `turnControllers` Map，`interruptSession` 升级为 `interrupted.add` **+ abort 该 controller + 移除**（取消进行中的 turn）；导出 `composeAbortSignals` 助手（不依赖 `AbortSignal.any`）。`ChannelBackendAdapter` 可选注入 lifecycle，`runTurn` 内 `lifecycle.bindTurnAbort(logicalSessionId, input.signal)` 组合 signal 透传给 runner 与工具桥——**未注入 / 无 logicalSessionId 时行为零变化**。`fusion-room-execution-bridge.recordUsage` 改用 `normalizeMemberTurnUsage`（authority 只收 inputTokens/outputTokens/costMicros，不收 wallTimeMs；normalize 后无可记账字段则不写账本）。新增门控真机冒烟 `member-session-lifecycle-kscc-smoke.test.ts`（`TAGENT_KSCC_LIFECYCLE_SMOKE=1`；不设 → skip；本机有 kscc 时实跑 2 pass：create→turn→heartbeat alive、interrupt 取消 inflight turn + heartbeat false）。**未做**：native resume（kscc bare 不 resume）、production service 注入 lifecycle（service 仍 `createChannelBackendAdapter()` 无 lifecycle，全量接线留后）、进程级 kill（由 runner 自身 `proc.kill` 处理，lifecycle 仅协作 signal）、实机 Electron GUI 手测。详见 [12-IMPLEMENTATION-LOG §79](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P1-2c 生产路径注入 lifecycle 已落地）**：`CollaborationRoomService` 默认栈与 Fusion runtime opt-in 现共享 `createDefaultChannelMemberStack()`（`member-backend-factory.ts`：`ChannelMemberSessionLifecycleAdapter` + `createChannelBackendAdapter(lifecycle)`，adapter 已 `bindTurnAbort` 到 lifecycle）。Service 默认装配该栈（生产 `registerCollaborationRoomIpc` 自动生效），`cancelRun` running 分支 fire-and-forget 调 `interruptSession({ handle:{ sessionId: member.logicalSessionId, ... }, reason:'cancel-run' })`（忽略 NOT_FOUND），排队/awaiting 分支不污染 session；只注入 adapter 不注入 lifecycle → 旧行为（兼容既有测试）。Fusion runtime 新增 `enableDefaultMemberExecution?: boolean`（默认 false，未传 memberAdapter 时自动装配默认栈 + executionBridge；显式 memberAdapter 优先）。新增 `memberLifecycle` getter。**未做**：native resume、createSession-before-every-turn（cancel 经 bindTurnAbort+interruptSession 仍可取消 turn，但 heartbeat 对未 createSession 的 session 返回 unknown）、持久 outbox worker、实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §80](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
2. 持久化 scheduler/outbox worker：服务离线期间已批准 approval、已回复 mailbox 和未投递消息要可观察；涉及文件/命令/网络副作用的 continuation 必须先进入显式 resume/confirm 状态，不能直接自动重放。
   - **旁注（2026-08-23，P1-1 可观察 outbox + 用户确认 resume 已落地，不自动重放）**：`packages/core/src/collaboration/fusion-room-continuation.ts` 已交付纯函数 `listFusionContinuations(snapshot)`（blocked run / pending approval / approved_awaiting_resume / mailbox_outbox / awaiting_peer / depth_stop，终态不列出）+ 单测；`FusionRoomHost` 新增 `listContinuations(roomId)` 与 `confirmResumeContinuation(input)`（active 守卫，幂等），`FusionRoomAuthority` 新增 `confirmResumeContinuation` 写 `run.resume_confirmed` / `mailbox.changed(resume_dispatched)` 事件——**不改 run 状态、不复活旧 fence**，`mailbox_outbox` 仅当 `canTransitionCollaborationDelivery(outbox→dispatched)` 时推进 delivery，`outcome_unknown` 等终态不得回退；`recoverInterruptedRuns()` 保持 running→blocked、不自动 confirm。**未做**：execution bridge 以新 turn 接线、持久 outbox worker 进程、远程 UI / gateway wire、真实 provider resume。详见 [12-IMPLEMENTATION-LOG §76](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P1-1b bridge + 远程页 UI 已接线，仍不自动重放）**：`FusionRoomExecutionBridge.handleAction` 已接 `confirm-resume-continuation`——`blocked_run` 以**新** runId / fence 拉起新 turn（旧 run 仍 blocked、旧 fence 不复活，prompt 注明「用户已确认继续此前被中断的工作」），`mailbox_outbox` 以 `resume-mailbox:<id>` 唤醒 toMember 新 turn；`already_confirmed` 用稳定 executionKey 幂等不双开。`FusionRoomGatewayAction` 新增 `confirm-resume-continuation`（payload 不含 actorUserId，principal 注入，`withScopedIdempotency` 作用域隔离）；view-model 投影 `continuations`（调 `listFusionContinuations`，经 `@tagent/core` 导出）；`FusionRoomActionAdapter.confirmResumeContinuation({ continuationId, kind, idempotencyKey? })`（roomId 由 view 注入）；`FusionRoomRemotePage` 在审批 / mailbox 区块下新增「待确认续跑」最小 UI（`requiresUserConfirm && (blocked_run|mailbox_outbox)` 出「确认继续」按钮，busy / error 态，确认后 snapshot 刷新）。**未做**：持久 outbox worker 进程、本地 legacy CollaborationRoomService 同等 UI、真实 provider resume、实机点远程页手测（无 Electron GUI，以单测 + typecheck 验证）。详见 [12-IMPLEMENTATION-LOG §77](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **旁注（2026-08-23，P1-3 持久 outbox worker 已落地，安全 drain、不自动重放）**：`FusionRoomOutboxWorker` + `classifyOutboxDrain` 已交付——`recoverInterruptedRuns` 后扫描 continuation，仅 `approved_awaiting_resume`（bridge `resolve-approval` 拉新 turn）与 `mailbox_outbox(delivery==='outbox')`（房主 `confirmResumeContinuation` 推进 dispatched + bridge `confirm-resume-continuation` 唤醒 toMember）安全自动 drain，其余 observe；`blocked_run` 永不写 processed-as-auto，`mailbox_outbox` re-read 非 outbox → observe 不重投。已 drain 键 `roomId:kind:continuationId` 原子持久化避免重启双开；runtime 构造期 `drainAll` 一次，挂 `runtime.outboxWorker`（始终挂载，无 bridge 仅 scan/observe），**不默认开短轮询**；新增 `outboxWorkerStatePath` 选项。与 legacy `CollaborationRoomService.recoverInterruptedRuns` 的 outbox 安全重投语义对齐（都只重投尚未启动模型调用的 outbox 信封），但本切片不改 legacy 路径。未做：legacy 共用 worker、IPC 暴露 scan、轮询默认开、真实 provider resume、实机手测。详见 [12-IMPLEMENTATION-LOG §81](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
3. running/awaiting/queued 的崩溃恢复、fence、取消、超时和错误状态在 UI 中可解释。
4. Git worktree/任务副本、文件租约跨进程实现和多文件 diff/人工合并。
5. 横向 prompt injection 的更完整来源隔离、工具参数 schema、附件/文件来源 ACL。

### P2：体验和覆盖

1. 远程页面的完整 tasks/artifacts/approvals/mailbox 操作体验，目前主要是最小投影和 action facade。
2. title/goal 编辑 UI、任务/产物/run 的详情和分页/动态高度虚拟列表。
3. Bot 替换、删除、协调者转移、owner 离线、席位 handoff 的全链路 UI 和恢复测试。
4. Sidecar 到正式成员/标签页的状态迁移、重启恢复和跨窗口异常处理。
5. Bot 长期 memory 的自动整理、压缩、版本回滚和与普通/融合 prompt 的真实检索 E2E。

- **状态（2026-08-23，P2-2 房间 title/goal 编辑 UI 已落地，远程 owner-only 仅 UI 闸）**：本地 `CollaborationRoomsPage` 补目标编辑（与 rename 同等权限/模式，空串可清空，归档禁用）；远程 `FusionRoomRemotePage` 补标题 + 目标 owner-only 弹层编辑经 `actions.updateMetadata`，header goal 独立一行（移出 debug 串）。view-model 投影新增 `canEditMetadata`（snapshot owner 与 session `actorUserId` 比较，**仅 UI 闸**，authority 仍 enforce owner-only + active）；`CollaborationTextPrompt` 扩 `multiline`/`allowEmpty`/`pending`/`error`。**诚实边界**：连接对话框不收集 actorUserId（无客户端账户认证，不做 OAuth）→ 生产环境 `canEditMetadata` 恒 false，远程编辑按钮不渲染（owner/non-owner 闸门逻辑已由单测覆盖，待真实账户接通即激活）；本地编辑完全可用。不改 authority/gateway 协议语义。未做实机 GUI 手测、idempotencyKey、跨机器 E2E。详见 [12-IMPLEMENTATION-LOG §84](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
- **状态（2026-08-23，P2-UX 单会话↔协作室桥接契约层已落地，仅契约层）**：`packages/shared/src/types/session-collab-bridge.ts` 已交付类型 + 预算常量 + 裁剪/校验纯函数 + 单测（27 pass）——`buildSessionToRoomBrief` / `formatSessionToRoomBriefForPrompt` / `buildRoomToSessionHandoff` / `formatRoomToSessionHandoffForPrompt` / `clampBridgeText` / `validateSourceExcerptBudget`；**仅契约层**，未做 UI / LLM summarize / upgrade·exit 主路径接线。规格见 [14-SESSION-COLLAB-BRIDGE-SPEC](./14-SESSION-COLLAB-BRIDGE-SPEC.md)，详见 [12-IMPLEMENTATION-LOG §85](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
- **状态（2026-08-23，P2-UX 桥接服务层已落地，UI 确认仍未做）**：`session-collab-bridge-service.ts` 已交付明示进房 / 明示退出 / 按需读原史服务 + IPC（`enter-with-bridge` / `exit-with-bridge` / `read-source-excerpt`，`userConfirmed` 闸）+ preload / App.tsx 类型同步 + 单测（8 pass，假 modelCaller，无真网）：enter 精炼单会话面板历史为前情提要写房间 goal + 系统消息，exit 精炼协作结论写回原 session 面板（系统通知卡）+ 清 `fusionRoomId` + 按剩余 Bot 重算 `fusionMode` + 房间 `paused`，excerpt 预算校验 + 读 panel + clamp；模型失败 fail-closed 启发式。**未做**：UI 开启/退出确认弹窗、host 工具接线（按需读原史工具回路）、`session_meta_changed` 推送（exit 后 meta 落盘 renderer 下次读生效）、关旧 `upgrade-from-session` 静默路径（旧路径仍无精炼桥，新路径须走 `enter-with-bridge`）。规格见 [14-SESSION-COLLAB-BRIDGE-SPEC](./14-SESSION-COLLAB-BRIDGE-SPEC.md) §5，详见 [12-IMPLEMENTATION-LOG §86](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
- **状态（2026-08-24，P2-UX 桥接 UI 层已落地，host 工具仍待）**：`SessionBotBar` 选满 ≥2 Bot 不再静默 `upgradeFusionSessionToRoom`，改「开启协作」→ `DestructiveConfirmDialog` → `enterCollaborationWithBridge({ userConfirmed:true })` + 派发 `tagent:session-meta-changed` + toast.success（旧静默升级路径从此 UI 断开）；`CollaborationRoomsPage` 头部加「结束协作」（`room.sourceSessionId` 匹配当前会话时显示，与归档图标区分）→ 确认 → `exitCollaborationWithBridge` + `session-meta-changed` + `onCollaborationExited` + toast；`Chat` 传 `sourceSessionId` / `onCollaborationExited`（meta 变更后 `usePersistedSessionMeta` 重读 `fusionRoomId` 自动切壳）。失败抛错由确认框内联提示（对齐 `SessionSidebar.deleteSession` 约定，不额外 toast.error 避免双显）。新增 `SessionBotBar.test.tsx`（3 pass）。**未做**：host 工具接线（`read-source-excerpt` 工具回路）、成员减到 1 的可选 toast 提示（不自动 exit）、实机 GUI 手测。规格见 [14-SESSION-COLLAB-BRIDGE-SPEC](./14-SESSION-COLLAB-BRIDGE-SPEC.md) §5，详见 [12-IMPLEMENTATION-LOG §87](./12-IMPLEMENTATION-LOG-2026-08-22.md)。

## 8. 推荐继续开发顺序

不要先打开公网入口，也不要先重写 UI。推荐顺序：

1. 先为认证/ACL 写协议和测试：principal、room scope、Bot owner consent、resource grant、费用主体。
   - **状态（2026-08-23）**：P0-1 已落地，见 §7 旁注与 [12-IMPLEMENTATION-LOG §72](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
2. 用内存/本地 fixture 做两个用户、两个 Bot owner、一个共享工作区的 HTTP/SSE E2E。
   - **状态（2026-08-23）**：本地 loopback fixture E2E 已落地（P0-2，`fusion-room-multiuser-fixture.test.ts`，见 [12-IMPLEMENTATION-LOG §73](./12-IMPLEMENTATION-LOG-2026-08-22.md)）；跨机器 / 真实账户 / 证书版本仍待第 3 步之后。
3. 完成证书和打包网络配置后，再把 packaged gate 变成显式用户操作；默认仍关闭。
   - **状态（2026-08-23）**：P0-3 已落地——证书生命周期 store（`fusion-room-cert-store.ts`）+ 显式闸门决策函数（`fusion-room-network-prefs.ts` `decidePackagedCollaborationGate`）+ 持久化偏好 + 始终注册的 prefs/certs 管理 IPC；`main/index.ts` 已用决策函数替代硬编码 `!app.isPackaged`，**默认 prefs 全关 → 打包行为不变**。`allowNonLoopbackListen` 已能正确产出（active 证书 + 双开关），但远程 transport 尚未从 Electron 启动接通该信号；详见 [12-IMPLEMENTATION-LOG §74](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P0-3b 显式用户操作 UI 已落地，默认仍关）**：设置页「关于」底部已挂「融合会话 · 协作与网络（危险区）」——两个 Switch（`enableCollaboration` / `enableNetworkListen`，后者在 `!enableCollaboration` 或 `!hasActiveCert` 时 disabled）+ 证书列表（短显指纹 / 状态 / 过期 / 撤销）+ 生成自签证书 + 只读闸门状态卡。新增 `fusion-room-network-prefs:gate-status` IPC（返回当前决策 + 启动应用决策 + `needsRestart` + `isPackaged`），preload + App.tsx 同步暴露 `getFusionRoomGateStatus`。**应用策略选 B**：所有闸门变更提示「重启应用后生效」+ 待重启徽章，不动态注册 / 卸载协作室 IPC（理由见 [12-IMPLEMENTATION-LOG §75](./12-IMPLEMENTATION-LOG-2026-08-22.md)）。**默认仍全关、不自动起非 loopback 监听**；未做实机点击手测（无 Electron GUI），以组件模型单测 + typecheck 验证。远程 transport 从 Electron 启动接通 `allowNonLoopbackListen` / 真实账户 / 生产 CA 仍未做。
   - **状态（2026-08-23，P0-3c transport 启动接通已落地，默认仍关）**：`bootstrapFusionRoomTransport`（`fusion-room-transport-bootstrap.ts`）把 `allowNonLoopbackListen` 接到 Electron 启动路径——据闸门 + cert store active TLS 材料 `createFusionRoomTransportRuntime` 并 `start`：`allowNonLoopbackListen` + TLS → HTTPS 非 loopback `listening`；无 TLS → `failed`（不得明文非 loopback）；`!allowNonLoopbackListen` → `disabled`（可选 `enableDevLoopbackTransport` 仅强制 127.0.0.1 明文）。`main/index.ts` 删 `console.log` 占位改真实 bootstrap + 日志 + `before-quit` close；新增 `fusion-room-transport:status` 只读 IPC（`appliedGate` + transport 投影）+ preload / App.tsx `getFusionRoomTransportStatus` + 设置页一行只读「传输：…」。`enableDefaultMemberExecution` 始终 false、fallback 认证 deny-all。**默认仍关**；未做真实账户 OAuth / 跨机器实机 E2E / 热插拔 listen / 实机 GUI 手测（以 bootstrap 单测 + 跨运行时 TLS 行为探测 + typecheck 验证）。详见 [12-IMPLEMENTATION-LOG §83](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
4. 实现持久 continuation 的“可观察 outbox + 用户确认 resume”，先不自动执行有未知副作用的旧 run。
   - **状态（2026-08-23，P1-1 已落地，不自动重放）**：可观察 continuation 列表（`listFusionContinuations` / `FusionRoomHost.listContinuations`）+ 用户确认 resume（`FusionRoomHost.confirmResumeContinuation`，写 `run.resume_confirmed` / `mailbox.changed(resume_dispatched)` 事件，**不**把旧 fence 的 blocked run 改回 running、**不**自动重放；outbox 仅 `outbox→dispatched` 合法前进，`outcome_unknown` 终态不得回退；幂等）+ 单测已交付。`recoverInterruptedRuns()` 仍只 running→blocked，不自动 confirm。**未做**：execution bridge 以新 runId/fence 拉起新 turn、持久 outbox worker 进程、远程 UI / gateway wire、真实 provider resume。详见 [12-IMPLEMENTATION-LOG §76](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P1-1b 已接线，仍不自动重放）**：execution bridge 已接 `confirm-resume-continuation`——`blocked_run` 以新 runId/fence 拉起新 turn（旧 run 仍 blocked，prompt 注明用户已确认继续），`mailbox_outbox` 唤醒 toMember 新 turn；`already_confirmed` 稳定 executionKey 幂等不双开。gateway 新增 wire action（principal 注入 actor，payload 不含 actorUserId）；view-model 投影 `continuations`；action adapter `confirmResumeContinuation`；远程页「待确认续跑」最小 UI（`requiresUserConfirm` 出「确认继续」按钮，busy/error 态，确认后 snapshot 刷新）。仍**不**自动重放、**不**复活旧 fence。**未做**：持久 outbox worker 进程、本地 legacy 同等 UI、真实 provider resume、实机手测。详见 [12-IMPLEMENTATION-LOG §77](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P1-3 持久 outbox worker 已落地，安全 drain、不自动重放）**：`FusionRoomOutboxWorker` 在 `recoverInterruptedRuns` 后扫描 continuation，仅对 `approved_awaiting_resume` 与 `mailbox_outbox(delivery==='outbox')` 安全自动 drain（前者 bridge `resolve-approval` 拉新 turn，后者 `confirmResumeContinuation` 推进 dispatched + bridge 唤醒 toMember），`blocked_run` / `pending_approval` / `depth_stop` / 已 dispatched 信封一律 observe、绝不自动重放；已 drain 键 `roomId:kind:continuationId` 原子持久化避免重启双开。runtime 构造期 `drainAll` 一次，挂 `runtime.outboxWorker`（始终挂载，无 bridge 仅 scan/observe），**不默认开短轮询**；新增 `outboxWorkerStatePath` 选项。与 legacy recover 的 outbox 安全重投语义对齐，不改 legacy。未做：legacy 共用 worker、IPC 暴露 scan、轮询默认开、真实 provider resume、实机手测。详见 [12-IMPLEMENTATION-LOG §81](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P2-1 本地 legacy 同等续跑 UI 已落地，不复活旧 fence）**：补齐 P1-1b 遗留的「本地 legacy 同等 UI」——本地 `CollaborationRoomService` 路径现与远程 Fusion 页对等。新增 shared 纯函数 `listLocalCollaborationContinuations`（6 种 kind + label map）+ service `listContinuations` / `confirmResumeBlockedRun`（**新建** turn 新 runId/fence，旧 blocked run 保持 blocked、fence 不变；幂等键 `resume-of:<oldRunId>:<uuid>` 避免与旧键 `trigger:member` 冲突，调用方键 `resume-blocked:<runId>` 使重复点击幂等）+ `collaboration-room:list-continuations` / `confirm-resume-blocked` IPC + preload + App.tsx 类型 + `CollaborationRoomsPage` 时间线上方续跑块（`blocked_run` 出「确认继续」，pending_approval / depth_stop 只读下钻既有卡片，其余观察）。仍**不**自动重放、**不**复活旧 fence、**不**大改远程 Fusion 页。未做：Fusion outbox scan IPC、确认后从列表移除旧 blocked run（设计如此，与远程一致）、实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §82](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
5. 接入真实 Pi/KSCC provider adapter，分别验证 session 生命周期和工具桥；不能用模型选择器存在来代替 provider E2E。
   - **状态（2026-08-23，P1-2a 契约层已落地，真机 E2E 未做）**：统一生命周期契约（create/resume/compact/interrupt/heartbeat）+ usage 规范化（`normalizeMemberTurnUsage`）+ Channel fail-closed 薄封装 + Fake + 单测已交付；Channel 的 resume/compact 诚实 fail-closed（`supportsResume=false`），**不**谎报。**未做**：真机 kscc/Pi create→resume E2E、进程级 kill、bridge 全量改用 normalize、实机手测。详见 [12-IMPLEMENTATION-LOG §78](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P1-2b interrupt 接 AbortSignal + bridge 接 normalize + 真机冒烟已落地，不声称 native resume）**：lifecycle `bindTurnAbort` 组合 signal + `interruptSession` abort controller 取消进行中的 turn；`ChannelBackendAdapter` 可选注入 lifecycle（未注入零变化）；bridge `recordUsage` 接 `normalizeMemberTurnUsage`（authority 不收 wallTime，无可记账字段不写账本）；门控真机冒烟（`TAGENT_KSCC_LIFECYCLE_SMOKE=1`，本机有 kscc 实跑 2 pass：create→turn→heartbeat alive、interrupt 取消 inflight turn + heartbeat false）。**未做**：native resume（kscc bare 不 resume）、production service 注入 lifecycle（全量接线留后）、进程级 kill（runner 自身处理）、实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §79](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
   - **状态（2026-08-23，P1-2c 生产路径注入 lifecycle 已落地）**：`createDefaultChannelMemberStack()` 共享工厂（`member-backend-factory.ts`，adapter 已 `bindTurnAbort` 到 lifecycle）；`CollaborationRoomService` 默认装配该栈（生产 `registerCollaborationRoomIpc` 自动生效），`cancelRun` running 分支 fire-and-forget 调 `interruptSession`（handle.sessionId=member.logicalSessionId，忽略 NOT_FOUND），排队/awaiting 分支不污染 session，只注入 adapter 不注入 lifecycle → 旧行为；Fusion runtime `enableDefaultMemberExecution`（默认 false，true+未传 memberAdapter → 自动装配默认栈 + executionBridge，显式 memberAdapter 优先）；新增 `memberLifecycle` getter + 7 新测（4 service + 3 runtime）。**未做**：native resume、createSession-before-every-turn（heartbeat 对未 createSession 的 session 返回 unknown）、持久 outbox worker、实机 GUI 手测。详见 [12-IMPLEMENTATION-LOG §80](./12-IMPLEMENTATION-LOG-2026-08-22.md)。
6. 最后补完整远程 UI、分页、Bot handoff、memory consolidation E2E。

## 9. 继续开发的工作流

在另一台设备：

```powershell
git clone https://github.com/Frank-LiangMX/TAgent-Desktop.git
cd TAgent-Desktop
git checkout main
git pull --ff-only origin main
bun install
bun run typecheck
```

如果仓库已经存在：

```powershell
git status --short
git pull --ff-only origin main
```

不要使用 `git reset --hard` 或 `git checkout --` 覆盖本地改动。先读本文件和最新 implementation log，再从 P0-1 继续。

每个切片应同时提交：

- 代码与回归测试；
- `03-IMPLEMENTATION-PHASES.md` 的当前状态或阶段验收；
- `12-IMPLEMENTATION-LOG-2026-08-22.md` 的新编号条目；
- 若改变产品边界，更新 `06-MULTIUSER-FUSION-IMPLEMENTATION.md` 和本交接文档。

完成一个切片后至少运行 typecheck、相关 Bun tests、build 或说明为何不能运行，并在交接日志中记录真实边界。不要声称没有做过的跨机器、真实 provider 或打包网络验证已经完成。

## 10. 本次交接提交范围

本次交接要求提交当前工作区已有的产品、融合会话、Bot、工作区、文档和测试改动，使另一台设备可以直接从远端继续。提交前应确认：

- 没有临时调试文件或凭证；
- handoff 文档、阶段文档、实现日志和代码在同一提交链中；
- `git diff --check` 通过；
- typecheck/build 通过；
- push 后用远端 commit 验证可见。