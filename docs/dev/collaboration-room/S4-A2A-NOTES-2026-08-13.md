# 协作室 S4 A2A · 实现 NOTES（2026-08-13）

> ⚠️ **更新（2026-08-17）：** S4-3a、**S4-3b（adapter 工具回路）**与 **S4.5** 均已交付，不再是「待做」。见 §4 底部增补与 [HANDOFF-2026-08-17](./HANDOFF-2026-08-17.md)。
>
> 分支：`feature/collab-room`
> 范围：S4 结构化 A2A，分两切片推进——**S4-1 纯逻辑层**（已交付）、**S4-2 host 侧信箱接线**（本次）；**S4-3 adapter 工具回路**待做。
> 上位：`docs/plans/agent-collaboration-room/02-RUNTIME-A2A-SPEC.md` §5–§9、§14

## 0. 30 秒结论

| 切片 | 内容 | 状态 | 可验证 |
|------|------|------|--------|
| S4-1 | mailbox 状态机 / 深度 / 自环 / 幂等 / 循环指纹（纯函数） | ✅ 已交付（`6bec97f`） | `vitest` 离线 |
| S4-2 | repository mailbox 落盘 + service `roomSend/Ask/Reply` + `markRunAwaitingPeer` + `recoverInterruptedRuns(awaiting→blocked)` + 循环指纹接入 + IPC/preload | ✅ 本次 | `vitest`（需 `bun install`） |
| S4-3a | host 侧 peer reply 唤醒：`roomReply` → 提问者 `awaiting_peer` 入队 continuation（幂等键含 requestId） | ✅ 本次 | `vitest` |
| S4-3b | adapter 工具回路：把 A2A 工具暴露给成员 turn；剩余 host 工具 | ✅ **2026-08-17 已交付**（kscc bare + Anthropic 协议外部渠道原生工具桥；四把工具 `room_send/ask/reply/task_update`） | `member-backend-adapter-external-tools.test.ts` |
| S4.5 | 信封作 outbox + 深度停止卡 + 继续一次 + 重启 `outcome_unknown` | ✅ **2026-08-17 已交付** | `collaboration-room-a2a.test.ts` / `collaboration-ipc.test.ts` |

**S4-2 的边界**：信箱语义 + 状态机 + 守卫可在无 adapter 工具回路下独立验证（落盘正确、自环/超深度/重复回复阻断、awaiting/恢复状态迁移合法）。**不**包含成员 turn 内主动调用 A2A 工具与 peer reply 唤醒——那是 S4-3。

## 1. S4-1 纯逻辑层（已交付）

`packages/shared/src/types/collaboration-a2a.ts`：

- `transitionCollaborationMailboxState(state, action)`：pending/delivered/answered/cancelled/expired 状态迁移，终态拒绝。
- `nextCollaborationA2ADepth(parentDepth, maxDepth)`：深度计算，超限 fail closed（硬上限，Agent 不可自行提高）。
- `isCollaborationSelfSend(from, to)`：自环守卫。
- `collaborationReplyIdempotencyKey` / `collaborationEnvelopeIdempotencyKey`：幂等键。
- `isCollaborationDuplicateReply(requestId, from, envelopes)`：重复回复检测（同一 request 仅一个有效终态回复）。
- `normalizeCollaborationPayload` / `collaborationHash32` / `collaborationLoopFingerprint`：近重复循环指纹（A→B→A 阻断）。

全部纯函数、无副作用，21 个断言手工校验通过；`collaboration-a2a.test.ts` 覆盖。

## 2. S4-2 host 侧接线（本次）

### 2.1 持久化

`apps/electron/src/main/lib/config/config-paths.ts`：新增 `getCollaborationMailboxPath()` → `~/.tagent[-dev]/collaboration/mailbox.json`。

`apps/electron/src/main/lib/collaboration/collaboration-room-repository.ts`：新增 mailbox 段（与 runs 同构）：
- `listMailboxByRoom` / `listActiveMailboxForMember` / `listMailboxByRequest` / `getMailboxEnvelope` / `appendMailboxEnvelope` / `upsertMailboxEnvelope` / `listMailboxByStatus`

### 2.2 service A2A 方法

`apps/electron/src/main/lib/collaboration/collaboration-room-service.ts`：

- `roomSend({ roomId, fromRunId, toMemberId, payload })`：异步通知，不暂停发送者；自环守卫；校验发送者深度；落 pending `message` 信封 + a2a_request 消息（visibility=participants）。
- `roomAsk({ roomId, fromRunId, toMemberId, question })`：提问；自环 + 深度守卫（`parentDepth` 由 `runDepth(runId)` 推导，**不接受外部传入**——安全字段宿主补齐红线）；落 pending `question` 信封（带 requestId）+ a2a_request 消息。
- `roomReply({ roomId, fromRunId, requestId, answer })`：回复；校验回复者=该 request 接收者（防伪造）；幂等阻断重复回复；落 `answered` reply 信封 + 原 question 信封置 answered + a2a_reply 消息。
- `markRunAwaitingPeer(runId)`：running → awaiting_peer（CAS）；释放调度器 slot + 成员回 idle + 广播 `run-awaiting-peer`。
- `runDepth(runId)`（私有）：从该 run 作为 causationId 已落盘信封取 max depth，无则 0。供深度守卫用。
- `listMailbox(roomId)` / `listPendingMailboxForMember(memberId)`：renderer 审计视图用。

`recoverInterruptedRuns()` 扩展：遗留 `awaiting_peer` run → `blocked`（continuation 已丢，02-spec §14 不自动重放，要求用户决定），与原 `queued/running → failed(INTERRUPTED)` 区分。

### 2.3 IPC / preload

- `collaboration-room-channels.ts`：新增 `LIST_MAILBOX` 通道 + `ListCollaborationMailboxInput`；`CollaborationRoomChangedPayload.kind` 加 `run-awaiting-peer` / `mailbox-updated`。
- `collaboration-ipc.ts`：注册 `LIST_MAILBOX` handler → `service.listMailbox`。
- `preload/index.ts`：暴露 `listCollaborationMailbox(roomId)` + 导入 `CollaborationMailboxEnvelope` 类型。

### 2.4 测试

`apps/electron/src/main/lib/collaboration/collaboration-room-a2a.test.ts`：10 个用例覆盖落盘/自环/深度/幂等/不匹配阻断/awaiting/恢复/房间非 active 拒绝/listMailbox。

## 3. S4-3a 已交付：host 侧 peer reply 唤醒（2026-08-16）

不依赖 pi-core 工具回路，即可在 service 层验证 A2A 闭环的「B 回复 → A 恢复」：

1. `markRunAwaitingPeer` 会 abort 物理 turn；`executeRun` 看到 abort 时若 run 已是 `awaiting_peer`/`done`/`blocked`，不再覆盖为 cancelled。
2. `roomReply` 成功后：若原 question 的 `causationId` run 处于 `awaiting_peer`，CAS 为 `done`，再入队 continuation（`a2a-continue:{requestId}:{askerMemberId}`）。
3. continuation 的 trigger 是刚落盘的 `a2a_reply` 消息；`buildTurnPrompts` 注入「A2A 恢复」段 + 对方回复正文，并声明勿重复副作用。
4. 提问者仍在 `running`（未 mark awaiting）时不唤醒——只落盘 reply。
5. 同 request 第二次 `roomReply` 仍被幂等阻断，不会产生第二条 continuation。

同期附带（体验债，非 A2A 协议）：

- `updateMember` + IPC `UPDATE_MEMBER`：UI 点击成员气泡改显示名 / 渠道 / 模型。
- 时间线成员气泡走 `MessageResponse` Markdown；A2A 提问/回复单独样式。

## 4. S4-3b 已交付（2026-08-17）：adapter 工具回路

> **本条已在 2026-08-17 关闭**：`blocked-on-pi-core` 已打通。kscc bare（`createKsccBareStreamFn`）与 Anthropic 协议外部渠道（`createHttpDirectStreamFn` + `isAgentCompatibleProvider`）现在都能把 `room_send/room_ask/room_reply/room_task_update` 四把受限 AgentTool 经真实 tool-use 协议暴露给成员 turn；`execute` 转发宿主 `hostToolHandler` 真实校验/落盘，`room_ask` 成功且 `awaitPeer` 时 `abortAgent` 停 loop 让 service 置 `awaiting_peer`。外部无原生工具能力渠道（openai/google/zhipu 等）仍走纯文本 runner（fail-closed）。测试：`member-backend-adapter-external-tools.test.ts`（schema 限权 + 转发 + result 回注 + unsupported fail-closed）。
>
> 以下是 2026-08-13 写下时的原「待做」规划，保留作历史参考；剩余未做 host 工具仅 `room_publish_artifact` / `room_request_user`。

S4-2/S4-3 边界已重划（见下）。本次又补了**循环指纹接入**（不依赖 backend，可验证）：

### 3.1 已并入本次（循环指纹接入）

`roomAsk` 现在检测 A↔B 近重复问答（02-spec §9）：
- **正向重复**：A→B 已问过同样问题（正文归一化后指纹碰撞）→ 阻断重复投递。
- **反向循环**：B→A 已问过同样问题（A→B→A）→ 阻断。

用 `collaborationLoopFingerprint`（S4-1）扫该房间已有 `question` 信封，指纹碰撞即 fail closed。测试覆盖在 `collaboration-room-a2a.test.ts` 的"循环阻断 / 正向重复阻断"两个用例。

### 3.2 已交付（2026-08-17）：原「blocked-on-pi-core」清单

原 8-13 记录的待做项现均已落地，保留对照：

1. ~~`MemberTurnInput` 加 `hostTools` 回调，adapter 桥接 function-calling~~ → 现为 `MemberTurnInput.hostToolHandler`（`{ roomSend, roomAsk, roomReply, roomTaskUpdate }`）；`room_ask` 成功后 adapter 经 `awaitPeer` 触发 `abortAgent`，service 据此 `markRunAwaitingPeer`。
2. `room_task_update` → ✅ 已实现（见 `collaboration-room-task.test.ts`）；**`room_publish_artifact` / `room_request_user` 仍未实现**。

红线（不得降级）：
- 不得用 systemPrompt 文本协议代替结构化工具（02-spec §5 / 03-phases §12）。
- 安全字段（rootMessageId/causationId/depth/fromMemberId）始终宿主补齐。
- 唤醒的 continuation run 必须用含 `requestId` 的幂等键，避免同一 reply 重复唤醒。

## 4. 验证清单（明日在工作电脑）

```bash
git checkout feature/collab-room && git pull
bun install   # 装依赖（本机 npx/tsc/vitest 此前缺失）
# S4-1 纯逻辑
npx vitest run packages/shared/src/types/collaboration-a2a.test.ts
# S4-2 host 侧
npx vitest run apps/electron/src/main/lib/collaboration/collaboration-room-a2a.test.ts
# 全量回归
npx vitest run packages/shared
npx vitest run apps/electron
bun run typecheck
bun run --filter='@tagent/electron' build:main
bun run --filter='@tagent/electron' build:preload
```

预期：
- S4-1 / S4-2 新测试全绿。
- 既有 collaboration-room-run/multi/scheduler/repository 测试不应回归（S4-2 只新增方法 + 扩展 recoverInterruptedRuns，未改 S2/S3 路径）。

## 5. 本机开发环境限制

本机无网络、`node_modules` 缺 `@tagent/pi-core` / `tsc` / `vitest`，故 S4-2 未实跑测试。已用 `bun build --no-bundle` 对全部 6 个改动文件做语法校验通过；测试逻辑经手工推演。**明日在工作电脑 `bun install` 后实跑为准**；若测试失败，最可能点是 mock adapter 的 run 状态时序（hanging adapter 下 run 是否真的进了 `running`），届时按 vitest 输出微调 `triggerRunningRun` 即可。