# SLICE-1 · DONE（CLI 工人配置层）

- **改了哪些文件**：新增 `packages/shared/src/types/cli-workers.ts`（类型+`isValidCliWorkersConfig`/`validateCliWorkersConfig`+`CLI_WORKERS_DEFAULT_SEED`+`shouldUseCliWorker`/`resolveDefaultWorker`+黑名单 hermes/openclaw）、其 `cli-workers.test.ts`、`apps/electron/src/main/lib/agent/cli-workers-service.ts`（`listCliWorkersConfig` 无文件 seed / `writeCliWorkersConfig` 校验后原子写）+其 `.test.ts`；改 `packages/shared/src/types/index.ts`（re-export）、`packages/shared/src/types/agent.ts`（`AGENT_IPC_CHANNELS.LIST_CLI_WORKERS`/`SAVE_CLI_WORKERS`）、`apps/electron/src/main/lib/config/config-paths.ts`（`getCliWorkersPath`）、`apps/electron/src/main/lib/ipc/session-service.ts`（LIST/SAVE handlers，SAVE 校验失败抛中文 Error）、`apps/electron/src/preload/index.ts` + `apps/electron/src/renderer/App.tsx`（bridge 方法 + electronAPI 类型）。默认 seed `enabled:false`/`defaultBackend:'in-process'`，零行为变化；未接 task、未 spawn、未做设置 UI。
- **如何跑测**：`node node_modules/vitest/dist/cli.js run packages/shared/src/types/cli-workers.test.ts apps/electron/src/main/lib/agent/cli-workers-service.test.ts` → 36 passed。typecheck：`node node_modules/.bun/typescript@5.9.3/node_modules/typescript/bin/tsc --noEmit -p packages/shared/tsconfig.json`（clean）；`-p apps/electron/tsconfig.json` 仅余 3 处与本 slice 无关的既有 WIP 报错（`Chat.tsx` modelId/fallbackModelId、`SessionSidebar.tsx` onClick，均来自分支 `onNew→newSession(workspaceId)` 半成品重构，非本 slice 引入）。
- **遗留**：(1) spawn/observer/runner 未实现（后续 slice）；(2) 设置 UI 未做（SLICE-3）；(3) `shouldUseCliWorker`/`resolveDefaultWorker` 已导出但暂无消费方（runner 落地后接）；(4) 黑名单为 basename 精确匹配（大小写不敏感），`hermes.exe`/`hermes-cli` 不命中，若需更严可再收紧；(5) 未 git commit（按要求）。
