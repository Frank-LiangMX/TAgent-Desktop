# 交接：2026-08-07 concise / Chat 权限 / 思考链闪没

> **明天换设备：先读本文件。**  
> 分支：`main`（提交见 git log）  
> 规格总览：`REGRESS-2026-08-07-SPEC.md` + 本目录 `REGRESS-*-FINDINGS.md`

---

## 一句话现状

今晚把 Chat 写拦英文打断、进度 live、Cursor 节奏、思考 fold settle、**思考结束后从时间线消失** 都改进工作树并提交。  
**实机回归还没做完**（尤其 REGRESS-E 思考留存）——明天第一件事用 concise 长思考跑一轮对照清单。

---

## 明天怎么验（按优先级）

1. `bun install`（如需）→ `bun run dev`（或你们惯用的 electron 启动）
2. 新会话 **Work** + **concise**，发一条会触发：长思考 → Read/Grep → Bash → 再思考 → 改文件 → 最终正文

| # | 期望 | 对应 |
|---|------|------|
| 1 | Chat 下 Write → 中文「讨论模式不能写入 / 切 Work」，**不是**英文 `doesn't want to proceed` | A |
| 2 | 工具间进度短文 **live 就出现**，不憋到整轮结束 | B |
| 3 | 同阶段工具聚成「探索了 N…，运行了 K」；少刷「思考了 1s」 | C |
| 4 | 思考流式钉底跟随；结束后 settle 再折成「思考了 Ns」，可再点开 | D |
| 5 | **结束后时间线仍有「思考了 Ns」灰字头**，不能只剩「运行了 N 条命令」 | E（最关键，昨晚截图失败点） |

失败时：把会话 messages jsonl + 截图丢回，对照下方 FINDINGS 的根因表。

---

## 批次对照（已实现 / 文档）

| ID | 问题 | 状态 | 读什么 |
|----|------|------|--------|
| A | Chat+Write → 英文拒工具打断 | 代码+测 | `REGRESS-A-FINDINGS.md` |
| B | 进度总结 live 不显示 | 代码+测 | `REGRESS-B-FINDINGS.md` |
| C | 节奏碎：思考刷屏 + 工具拆行 + live 弹跳 | 代码+测 | `REGRESS-C-FINDINGS.md` |
| D | 思考滚动不跟 + 秒折 | 代码+测 | `REGRESS-D-FINDINGS.md` |
| E | 思考结束后整段从时间线消失 | 代码+测（**待实机**） | `REGRESS-E-FINDINGS.md` |

产品契约：`CURSOR-CONCISE.md`。

---

## REGRESS-E 根因备忘（换设备必看）

若明天仍「只剩运行了命令」：

1. kscc **不打** `_partial` → 已在 `kscc-message-adapter` 用「无 `stop_reason` ⇒ partial」推断，并透传 `stop_reason`
2. 同 uuid tool-only upsert 剥思考 → `preserveAssistantThinking`
3. `tool_start` 曾整表 `resetStreamState` → 现只清 text，保留 thinking
4. `turn_end`/`result` 清 stream 前 → `commitStreamThinkingToLastAssistant`

关键文件：

- `packages/shared/src/utils/kscc-message-adapter.ts`
- `apps/electron/src/renderer/components/chat/stream-item-model.ts`
- `apps/electron/src/renderer/components/chat/Chat.tsx`
- `ConciseTimelineView.tsx` / `concise-timeline-model.ts` / `session-turn-model.ts`

---

## 派工约定（额度）

探索/实现继续用本机 **`kscc -p --dangerously-skip-permissions`**，不要用 Cursor Task 烧额度。  
Brief 模板在本目录 `REGRESS-*-brief.md` / `*-kscc-prompt.txt`。

---

## 本轮明确不做 / 已知债

- 未强制验证：实机 Chat 硬拦 + 长思考结束后 fold 常驻（明天验）
- `investigation/kscc-stream-flush/` 为早期假设表，以 REGRESS-E FINDINGS 为准
- 若明日仍缺「探索了/编辑了」只见 shell：查该轮工具是否真的只有 Bash，或 `classifyToolFamily` 归类
- 曾有依赖本机 `~/.tagent/agent-sessions/*.jsonl` 的 REGRESS-E 回放测，**未入库**（换设备会挂）；以 `stream-item-model.vitest` / `kscc-message-adapter.test` 为准

---

## 快速命令

```bash
# 相关单测
bunx vitest run packages/shared/src/utils/kscc-message-adapter.test.ts \
  apps/electron/src/renderer/components/chat/stream-item-model.vitest.test.ts \
  apps/electron/src/renderer/components/chat/regress-b-progress-live.vitest.test.ts \
  apps/electron/src/renderer/components/chat/concise-timeline-model.vitest.test.ts \
  apps/electron/src/main/lib/permission/permission-service.test.ts
```
