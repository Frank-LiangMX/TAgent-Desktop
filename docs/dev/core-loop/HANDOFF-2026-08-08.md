# 交接：2026-08-08 concise 阶段性总结「流完即消」根治（REGRESS-N）

> **换设备 / 接手前先读本文件 + `REGRESS-N-FINDINGS.md`。**
> 分支：`main`（**未 commit**，工作树改动见下方）
> 规格：`docs/dev/core-loop/REGRESS-N-progress-vanish-stubborn-brief.md`
> 取证：`docs/dev/core-loop/REGRESS-N-FINDINGS.md`

---

## 一句话现状

REGRESS-J(J1/J4) 的 `isShortIdleProgress → continue`（按长度 ≤20 一刀切）在 idle 重投影时把有信息的段间短 progress 丢掉，撤销了 REGRESS-M 的 `tool_start` commit → 「阶段性总结流完即消」。已改写为「仅纯 filler 可吞；有信息 progress 常驻 `narrative.progress`」，live/idle 同一套 segments 语义。vitest 166 绿、typecheck 0 错。**未实机回归、未 commit。**

**2026-08-08 续 REGRESS-O（UI remount/折叠吞，非丢数据）**：O1—`pushNarrative` narrative key 改位置下标 `narrative-${index}`，跨 stream→partial→commit 不再换 key remount（旧因 `stream-text`→`text-${owner}-${i}` 换 key 致 NarrativeRow 秒空+重播）；O2—`WorkStageFold` 末步 live 思考正文打字机常挂阶段摘要下（收起也可见，hold→淡出折进 steps），不再只扫光「正在思考…」。改 `concise-timeline-model.ts` / `ConciseTimelineView.tsx` / `chat.css` + 新 `regress-o-ui-swallow.vitest.test.ts`（5 用例）；chat 套件 171 绿、typecheck 0 错。取证见 `REGRESS-O-FINDINGS.md`，**未实机回归、未 commit、未碰 MoA**。

---

## 已改（工作树，未 commit）

| 文件 | 要点 |
|------|------|
| `concise-timeline-model.ts` | 新增 `isFillerProgressText`（`:400-417`）；text 分支改 filler-only 跳过、有信息 progress 常驻（`:617-636`）；否决 J1 `isShortIdleProgress` |
| `concise-timeline-model.vitest.test.ts` | J4 用例改 filler+合并；新增「idle ≥2 progress」+ filler 合并两用例 |
| `regress-b-progress-live.vitest.test.ts` | idle 断言改保留 2 progress + 1 final（否决 J4 idle-drop） |
| `ConciseTimelineView.tsx` | 「· 含思考」提示（`:436-439,491-493`）；progress 空内容 `return null`（无内容勿占位，`:770-783`）；顺带修预存 `stepDurationSec` TS 错（`:653-656`） |
| `REGRESS-N-FINDINGS.md` / 本文件 / `REGRESS-J-FINDINGS.md` | 取证 + 交接 + J 否决注记 |

**未动**：`stream-item-model.ts`、`Chat.tsx`、`session-turn-model.ts`、`chat.css`、所有 MoA 文件。

---

## 明天怎么验（按优先级）

1. `bun install`（如需）→ rebuild + 重启 TAgent-Desktop（**排除 build-lag**）→ 新会话 **concise** 模式。
2. 发「长思考 → 工具 → 段间短总结 → 工具 → 段间短总结 → 工具 → 最终正文」型任务。

| # | 期望（对照 2026-08-08 截图失败点） | 对应 |
|---|------|------|
| 1 | 段间短总结 live 打字机即时可见 | B |
| 2 | **idle 后短总结仍在**为深色 progress 短文，不「流完即消」 | **N（本轮主修）** |
| 3 | 阶段灰字行与 progress 短文交替；含思考阶段摘要带「· 含思考」，点开见完整思考 | N |
| 4 | 阶段之间 / 阶段与正文之间**无大空白** | N |
| 5 | filler「好的/继续」夹在连续 Bash 之间 → idle 合并成单一「运行了 N 条命令」 | N 验收 2 |

失败时：抓该轮 live `.messages.jsonl`（`~/.tagent*/projects/<proj>/session-*.messages.jsonl`）+ 截图，对照 `REGRESS-N-FINDINGS.md` §1 四问。

---

## 跑命令

```bash
# 单测
npx vitest run apps/electron/src/renderer/components/chat/
# typecheck
cd apps/electron && bun run typecheck
```

---

## 关键根因备忘（换设备必看）

若仍「阶段性总结流完即消」：

1. **先确认运行 App 含本轮改动**（build-lag 是 REGRESS-K 反复踩的坑）。
2. idle 重投影路径：`buildConciseTimeline(isLive=false)` text 分支——确认走的是 `isFillerProgressText`（filler 才 `continue`），**不是**旧的 `isShortProgressText`（长度一刀切）。若仍按长度丢，说明跑的是旧 build。
3. `Chat.tsx:1360-1367` `tool_start` 的 `commitStreamTextToLastAssistant` 把段间 progress 插到 tool_use 前——确认 text 块进了末条 assistant content（`stream-item-model.ts:197-201`），且未被 `answerOverlay` 前缀去重吃掉（`session-turn-model.ts:781-802`，边角）。
4. 「大空白」若实机仍复现且非空占位：抓 live `.messages.jsonl` 看 `text` 块是否被 `answerOverlay` 去重，或 `useSmoothStream` 收缩过渡留白（本轮未动打字机 lib）。

关键文件：
- `apps/electron/src/renderer/components/chat/concise-timeline-model.ts`
- `apps/electron/src/renderer/components/chat/ConciseTimelineView.tsx`
- `apps/electron/src/renderer/components/chat/stream-item-model.ts`（只读）
- `apps/electron/src/renderer/components/chat/Chat.tsx`（tool_start 只读）
- `docs/dev/core-loop/REGRESS-N-FINDINGS.md` / `REGRESS-J-FINDINGS.md`

---

## 派工约定

探索/实现继续用本机 `kscc -p --dangerously-skip-permissions`，不用 Cursor Task 烧额度。本轮**未 commit**；确认实机绿后再由用户决定提交。避开无关 MoA 工作树改动。
